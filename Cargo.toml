[package]
name = "hollandaise"
version = "0.2.0"
edition = "2021"

[dependencies]
pulldown-cmark = "0.12"
automerge = { version = "0.7", optional = true }

[features]
default = []
automerge = ["dep:automerge"]

[dev-dependencies]
automerge = "0.7"
